dependencies:
  # Pinned to an exact version for reproducible builds. Bump intentionally after
  # verifying the GCC14 workaround and API compatibility in CMakeLists.txt.
  espressif/esp_matter: "==1.4.0"
