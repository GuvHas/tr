# Name,      Type, SubType, Offset,   Size,   Flags
# Two NVS partitions to isolate Matter credentials from general app state:
#   nvs        — general app state; safe to auto-erase on corruption.
#   nvs_matter — Matter fabric credentials, Thread network data, ACL entries,
#                group keys. Never auto-erased; requires deliberate factory reset.
# Combined size unchanged (48 KB). phy_init offset unchanged at 0x15000.
# otadata removed: no ota_0/ota_1 partitions exist so it was non-functional.
nvs,        data, nvs,     0x9000,   0x6000,
nvs_matter, data, nvs,     0xF000,   0x6000,
phy_init,   data, phy,     0x15000,  0x1000,
factory,  app,  factory, 0x20000,  0x300000,
coredump, data, coredump,0x320000, 0x20000,
